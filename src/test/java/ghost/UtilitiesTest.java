package ghost;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

import java.util.*;

import java.io.FileReader; 
import java.util.Iterator; 
import java.util.Map; 
  
import org.json.simple.JSONArray; 
import org.json.simple.JSONObject; 

public class UtilitiesTest {
	@Test
	public void parseConfig() {
		/*
		JSONObject foo = Maps.parseConfig("config.json");
		String map = (String)foo.get("map"); 
		assertTrue(map.equals("map.txt"));

		String modeLengths = ((JSONArray) foo.get("modeLengths")).toString();
		assertTrue(modeLengths.equals("[7,20,7,20,5,20,5,1000]"));

		int lives = Integer.parseInt(foo.get("lives").toString());
		assertTrue(lives == 3);

		int speed = Integer.parseInt(foo.get("speed").toString());
		assertTrue(speed == 1);
		*/
	}
}
